version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cipherlink-app
    restart: always
    ports:
      - "80:80"
    environment:
      - ENVIRONMENT=production
      - POSTGRES_SERVER=127.0.0.1
      - POSTGRES_PORT=5432
      - POSTGRES_DB=postgres
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - REDIS_HOST=127.0.0.1
      - REDIS_PORT=6379
      - AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID:-AKIA45Y2RPBFNXKBOQOC}
      - AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY:-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}
      - AWS_REGION_NAME=${AWS_REGION_NAME:-us-east-1}
      - AWS_IMAGES_BUCKET=${AWS_IMAGES_BUCKET:-fastapijp}
